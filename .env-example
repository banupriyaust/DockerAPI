# Application Mode
MODE=development

# API Key for authentication
# For local development, use a simple key like: test-key-12345
# For production, use a strong, randomly generated key (minimum 32 characters)
# Example: openssl rand -hex 32
API_KEY=your-secure-api-key-here